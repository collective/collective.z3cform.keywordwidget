[buildout]
extends =
    https://raw.githubusercontent.com/collective/buildout.plonetest/master/test-5.2.x.cfg

package-name = collective.z3cform.keywordwidget
package-extras = [test]

[test]
defaults = ['-s', '${buildout:package-name}', '--auto-color', '--auto-progress']
